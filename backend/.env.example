# Most projects can run with code defaults only.
# Only keep entries here when you really need to override them.

DB_ENABLED=false
# DATABASE_URL=mysql://user:password@db:3306/database

# Optional overrides:
# APP_NAME=web tools
# ENVIRONMENT=local
# LOG_LEVEL=INFO
# CORS_ALLOW_ORIGINS=*
